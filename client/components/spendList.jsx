import React, { Component, PropTypes } from 'react';
import { Meteor } from 'meteor/meteor';
import { render } from 'react-dom';

SpendList = React.createClass({
    
render(){
    return(
        <div className="spendList">
            <h1>SpendList</h1>
        </div>
    );
}
});